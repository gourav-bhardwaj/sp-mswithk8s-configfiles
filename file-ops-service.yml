server:
  port: 8082
  servlet:
    context-path: /file-ops
    
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"    

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ 